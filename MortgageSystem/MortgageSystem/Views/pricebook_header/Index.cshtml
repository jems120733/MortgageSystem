@model IEnumerable<MortgageSystem.Models.inv_pricebook_header>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.crm_branch.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.crm_customer.first_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.inv_pricebook_type.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.mf_status.description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.crm_branch.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.crm_customer.first_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.inv_pricebook_type.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mf_status.description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
